## abacus entities

List or search entity nodes

### Synopsis

List all entity nodes in the graph, optionally filtering by FTS5 search query.

Results include node ID, name, label, and source file. Use --json for
machine-readable output.

```
abacus entities [flags]
```

### Examples

```
  $ abacus entities
  $ abacus entities --match users
  $ abacus entities --limit 10 --json
```

### Options

```
  -h, --help           help for entities
  -l, --limit int      Maximum number of results (0 = all)
  -m, --match string   FTS5 search query
```

### Options inherited from parent commands

```
      --db string   Path to the database file (default ".abacus/abacus.db")
      --json        Output in JSON format
      --quiet       Suppress non-essential output
      --verbose     Enable verbose output
```

### SEE ALSO

* [abacus](abacus.md)	 - Living application graph for Gherkin-driven BDD

###### Auto generated by spf13/cobra on 8-Mar-2026
