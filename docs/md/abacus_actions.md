## abacus actions

List or search action nodes

### Synopsis

List all action nodes in the graph with their linked route, entity, and page counts.

Use --match to search actions by name or label via FTS5. Results show how many
graph nodes of each kind are connected to each action.

```
abacus actions [flags]
```

### Examples

```
  $ abacus actions
  $ abacus actions --match "create user"
  $ abacus actions --limit 20 --json
```

### Options

```
  -h, --help           help for actions
  -l, --limit int      Maximum number of results (default 50)
  -m, --match string   FTS5 search query
```

### Options inherited from parent commands

```
      --db string   Path to the database file (default ".abacus/abacus.db")
      --json        Output in JSON format
      --quiet       Suppress non-essential output
      --verbose     Enable verbose output
```

### SEE ALSO

* [abacus](abacus.md)	 - Living application graph for Gherkin-driven BDD
* [abacus actions create](abacus_actions_create.md)	 - Create a new action node

###### Auto generated by spf13/cobra on 8-Mar-2026
